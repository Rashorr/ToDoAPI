ktor {
    deployment {
    port = 8080
    allowedHost = "localhost:8080"
    allowedScheme = "http, https"
    }
    database {
        driverClass = ""
        databaseUri = ""
        maxPoolSize = 10
        maxLifetime = 60000
    }

}